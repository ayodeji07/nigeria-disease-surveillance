# requirements-api.txt
# Production dependencies for the FastAPI server only.
# Excludes ETL, notebook, dashboard, and testing packages
# to keep the Docker image lean on Render's free tier.

# ── Core data ────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0

# ── Database ─────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# ── API framework ────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9

# ── Geospatial (accessibility + Moran's I endpoints) ─────────
geopandas>=0.14.0
shapely>=2.0.0
fiona>=1.9.0
pyproj>=3.6.0
libpysal>=4.9.0
esda>=2.5.0

# ── Analytics (forecast, trend, clustering endpoints) ────────
scipy>=1.11.0
statsmodels>=0.14.0
scikit-learn>=1.3.0
pymannkendall>=1.4.3
prophet>=1.1.5

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
tenacity>=8.2.0
loguru>=0.7.0
